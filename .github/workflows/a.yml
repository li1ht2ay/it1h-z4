on:
  workflow_dispatch:
  schedule:
    - cron: '45 18 3,7,11,15,19,23,27,31 * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: li1ht2ay/it1h-z4@cron
        with:
          token: ${{ secrets.token }}
