on:
  workflow_dispatch:
  schedule:
    - cron: '45 18 2,6,10,14,18,22,26,30 * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: li1ht2ay/it1h-z4@cron
        with:
          token: ${{ secrets.token }}
